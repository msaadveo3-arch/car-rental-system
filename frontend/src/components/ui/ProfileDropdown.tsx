import React from 'react';

const ProfileDropdown: React.FC = () => {
  return (
    <div className="relative">
      <button className="px-3 py-2 rounded bg-gray-100">Profile</button>
      {/* Dropdown placeholder */}
    </div>
  );
};

export default ProfileDropdown;
